 <h1>Goods Properies in BidGate: </h1>


 <% @goods_props.each do |prop| %>
  <li>
    <span><%= prop.name%> : </span> 
    <div style="padding-left: 50px;">
    	<% prop.goods_prop_values.each do |pv| %>    	
    		<span><%= pv.prop_value %></span>
    	<% end %>
    </div>
  </li>
<% end %>