<% flash.each do |name, msg| %>
	<% if name != 'notice' %>
	<%= content_tag :div, msg + '  X', :id => "flash_#{name}", :class => "msg_area error" %>
	<% else %>
	<%= content_tag :div, msg + '  X', :id => "flash_#{name}", :class => "msg_area" %>
	<% end %>
<% end %>